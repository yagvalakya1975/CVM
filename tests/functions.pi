int add(int a, int b) {
    return a + b;
}

void show(int value) {
    print(value);
}

int later(int n) {
    int saved = n;
    if (n <= 1) {
        return n;
    }
    return later(n - 1) + later(n - 2);
}

int first(int x) {
    return later(x);
}

print(add(2, 3));
show(first(6));
